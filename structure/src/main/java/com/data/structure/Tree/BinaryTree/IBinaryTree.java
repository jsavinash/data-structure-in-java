package com.data.structure.Tree.BinaryTree;

/**
 * A interface of binary tree.
 *
 * @param <T> The class type of the binary tree node.
 */
public interface IBinaryTree<T> {
    // void addData(T data);
}
